# Session Keymaps Configuration
# Format: key=directory
# The key is a single character that will be bound to ctrl-<key>
# The directory is the path relative to $HOME
# Example: g=.dotfiles (creates ctrl-g to open dotfiles session)

# Default sessions
g=.dotfiles
w=work
p=personal
