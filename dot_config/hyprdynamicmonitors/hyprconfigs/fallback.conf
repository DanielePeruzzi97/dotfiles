# Fallback profile - safety net for unknown monitor setups
# CRITICAL: Always enable laptop display first to prevent "no monitors" state
# This ensures recovery is always possible by opening the laptop lid
monitor=eDP-1,preferred,0x0,1

# Auto-configure any other connected monitors
monitor=,preferred,auto,1
