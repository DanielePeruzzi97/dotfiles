#!/usr/bin/env bash
# Enable systemd user services for Hyprland helpers — workstation only.
set -euo pipefail

{{ if ne .profile "workstation" -}}
exit 0
{{- end }}

if ! systemctl --user show-environment >/dev/null 2>&1; then
    echo "user systemd unavailable, skipping"
    exit 0
fi

systemctl --user daemon-reload

SERVICES=(
    "hypridle.service"
    "hyprpaper.service"
    "hyprpolkitagent.service"
    "hyprsunset.service"
)

for s in "${SERVICES[@]}"; do
    if systemctl --user list-unit-files --type=service --all | grep -q "^$s"; then
        systemctl --user is-enabled "$s" >/dev/null 2>&1 || systemctl --user enable "$s" || true
    fi
done
echo "Hyprland systemd services done"
