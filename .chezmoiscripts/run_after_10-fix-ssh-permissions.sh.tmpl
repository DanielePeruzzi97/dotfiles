{{- "" -}}
{{- /* SSH permissions script — disabled while testing chezmoi native private_ handling.
       Re-enable if SSH keys end up with wrong permissions after chezmoi apply.
       To re-enable: restore the original content from git history.
*/ -}}
